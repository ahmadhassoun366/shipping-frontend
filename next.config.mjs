/** @type {import('next').NextConfig} */
const nextConfig = {
    // configure all websites for images 
    images: {
        domains: ['raw.githubusercontent.com'],
    },
    // configure all websites for env variables
};

export default nextConfig;
